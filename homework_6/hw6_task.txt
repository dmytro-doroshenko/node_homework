Вам необхідно реалізувати функціонал логінації, логауту та рефреш токену.
3 ендпоінта, які починаються зі /auth.
На логінації вам видається пара токенів і пишеться в базу.
На логауті треться токен.
На рефреші генерується нова пара, а стара стерається.

/має бути моделька з токенами, валідатори на кожен ендпоінт, мідлвари з перевірками,  винесені в константи спільні змінні.